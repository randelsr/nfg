# experiments/

Empty inbox. One file per experiment, using the template in [../WIKI.md](../WIKI.md):
*hypothesis / why / method / status / conclusion & implications / disposition*.

An experiment that concludes and gets adopted **graduates** into a `decisions/` page (and this folder's
copy is left marked as superseded or pruned). Nothing here yet — nfg's build was a straight-line phased
implementation, not an experimental one.
