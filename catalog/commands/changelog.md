---
description: Draft a changelog entry summarizing the commits since the last release tag.
---

Look at the commits since the most recent git tag (or since the last
CHANGELOG.md entry if there is no tag). Group the changes into Added,
Changed, and Fixed sections, written for an end user rather than as raw
commit messages. Keep each bullet to one line. Print the drafted entry;
do not write it to a file unless asked.
